<nav class="navbar navbar-expand-lg">
  <a class="navbar-brand" href="#">
    <%= image_tag 'logo.png', class: 'logo', style: "width: 10rem;" %>
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="<%= admin_dashboard_path %>">Dashboard <span class="sr-only">(current)</span></a>
      </li>
      <% if current_user.present? %>
        <li class="nav-item">
          <a href="<%= orders_path %>" class="btn btn-secondary text-light">
            Productos <span class="badge badge-light" id="product-num"><%= @num %></span>
          </a>
        </li>
        <li class="nav-item">
          <%= link_to 'cerrar sesión', destroy_user_session_path, method: :delete, class:'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to 'catalogo', admin_catalogo_path, class:'nav-link' %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
